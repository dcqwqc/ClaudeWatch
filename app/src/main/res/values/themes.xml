<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Wear apps render their UI in Compose; this theme just provides a
         black launch window so there is no white flash before Compose draws. -->
    <style name="Theme.ClaudeWatch" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:colorBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
